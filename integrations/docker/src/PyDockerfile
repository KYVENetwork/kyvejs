# Use the official Python image as the base image
FROM python:3.9

# Set the working directory inside the container
WORKDIR /app

# Copy the Python code and protobuf files to the container
COPY src/* ./

# Install the required Python packages
RUN pip install --no-cache-dir -r requirements.txt

# Generate the protobuf Python files (if not already generated)
# Make sure you have the protobuf compiler (protoc) and grpcio-tools installed on your system
RUN python -m grpc_tools.protoc -I /app/ --python_out=/app/ --grpc_python_out=/app/ password.proto

# Expose the gRPC port
EXPOSE 50052

# Run the Python script
CMD ["python", "server.py"]